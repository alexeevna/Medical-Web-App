CREATE TABLE public.test_table_users (
                                id int PRIMARY KEY,
                                firstName varchar(255),
                                lastName varchar(255)
);